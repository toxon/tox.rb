language: ruby

rvm:
  - 2.3
  - 2.4
  - 2.5
  - 2.6

before_install:
  - sudo apt-get update
  - sudo apt-get install -y build-essential libtool autotools-dev automake checkinstall check git yasm cmake

  - sudo ./bin/build/libsodium
  - sudo ./bin/build/libtoxcore

  - sudo mkdir -p '/etc/ld.so.conf.d/'
  - echo '/usr/local/lib/x86_64-linux-gnu/' | sudo tee -a '/etc/ld.so.conf.d/locallib.conf'
  - sudo ldconfig

before_script:
  - rake compile
